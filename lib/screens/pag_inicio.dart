import 'package:flutter/material.dart';

class PagInicio extends StatelessWidget {
  final Color color;

  PagInicio(this.color);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: color,
    );
  }
}
